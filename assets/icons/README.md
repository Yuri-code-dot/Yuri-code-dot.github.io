# Icons

Reserved for standalone icon files (favicons, app icons, social icons).

All UI icons used across the site are authored as **inline SVG** directly in the HTML,
so no external icon files are required. Add optimized `.svg`/`.png` files here if you
prefer to externalize them, and reference them via `assets/icons/your-icon.svg`.
