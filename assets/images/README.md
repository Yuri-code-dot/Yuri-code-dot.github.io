# Images

Reserved for raster imagery (photographs, generated artwork, OG/social cards).

The current site relies on **inline SVG** illustrations and **HTML Canvas** for all
visuals, so this directory intentionally holds no binary assets. Drop optimized
`.webp`/`.svg` files here and reference them with relative paths such as
`assets/images/your-file.webp`.
